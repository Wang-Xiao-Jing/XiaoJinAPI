package xiao_jin.api.datagen;

/**
 * @author 尽
 * @apiNote 数据生成
 */
//@EventBusSubscriber(bus = EventBusSubscriber.Bus.MOD, modid = MOD_ID)
public class DatagenAPI {
//    @SubscribeEvent
//    private static void gatherData(GatherDataEvent.Client event) {
//        DataGenerator gen = event.getGenerator(); // 获取数据生成器
//        PackOutput packOutput = gen.getPackOutput(); // 获取输出路径
//        CompletableFuture<HolderLookup.Provider> lookupProvider = event.getLookupProvider(); // 获取查找提供者
//        gen.addProvider(true, new AdvancementProviderAPI(packOutput, lookupProvider, List.of())); // 生成成就文件
//        gen.addProvider(true, new LanguageProviderAPI(packOutput)); // 生成语言文件
//        gen.addProvider(true, new ModelProviderAPI(packOutput)); // 生成模型文件
//        gen.addProvider(true, new ParticleDescriptionProviderAPI(packOutput)); // 生成粒子描述文件
//        gen.addProvider(true, new SoundDefinitionsProviderAPI(packOutput)); // 生成粒子定义文件
//        gen.addProvider(event.includeReports(),
//                new LootTableProvider(packOutput, Collections.emptySet(),
//                        List.of(new LootTableProvider.SubProviderEntry(BlockLootTableProviderAPI::new, LootContextParamSets.BLOCK)),
//                        lookupProvider));
//    }
}
